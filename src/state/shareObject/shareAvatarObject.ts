import { Model } from '@src/types/avatar/ModelType';

// const model = <{ [peerId: string]: Model | any }>{};
const modelListObject: { [peerId: string]: Model | any } = {};

export { modelListObject };
